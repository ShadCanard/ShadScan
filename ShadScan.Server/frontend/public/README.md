# Public Assets

Place static assets here that should be served from the root of the frontend.

For example, a favicon file named `favicon.ico` should live at the top level of this
folder so that it is available at `/favicon.ico`. Replace the placeholder with your
actual icon graphic.
